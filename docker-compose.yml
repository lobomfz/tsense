services:
  typesense:
    image: typesense/typesense:28.0
    environment:
      TYPESENSE_DATA_DIR: /data
      TYPESENSE_API_KEY: 123
    volumes:
      - data:/data
    ports:
      - 8108:8108

volumes:
  data:
