services:
  typesense:
    image: typesense/typesense:28.0
    env_file: .env.test
    environment:
      TYPESENSE_DATA_DIR: /data
      TYPESENSE_API_KEY: ${TYPESENSE_API_KEY}
    volumes:
      - data:/data
    ports:
      - ${TYPESENSE_PORT}:8108

volumes:
  data:
